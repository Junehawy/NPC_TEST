# 审查报告：阶段3 寻路与感知增强（2026-08-17）

## 1. 变更范围

- **`npc_agent/testing/grid_nav.{h,cpp}`（新增）**：宿主侧 A* 示例——网格占用 + 确定性
  A*（开放表按 (f,g,行列) 全序，无平局随机）、can_reach/find_path、障碍版本号、
  origin 偏移（负坐标）、block_rect。
- **`npc_agent/testing/move_done_capability.h`（新增）**：action.completed → 单 tick
  move_done 脉冲（on_event 记录 → on_tick 置位/复位）。
- **headless 验收（test_grid_nav 8 用例 + test_stage3_nav 4 用例）**：绕障直行/不可达/
  确定性/版本号/坐标换算；沿 A* 移动 + 途中障碍重规划 + 动作事件推进 FSM + 脉冲语义。
- **Godot 接入**：GodotWorld 委托 GridNav（set_grid_nav）；GodotBody 路径跟随
  （set_path/consume_arrival/last_move_handle/is_moving/path_target/move_speed）；
  演示节点——网格按窗口重建（0.25u 单元）、建筑→网格障碍（register_map_obstacles）、
  每帧移动意图→A* 航点注入 + 到达回投（plan_paths_and_report）、E 键放置障碍触发
  移动中 NPC 重规划（place_obstacle）、MoveDoneCapability 注册（单/多模式）、提示语。
- **town 配置**：守卫巡逻 walk1/walk2、支援兵响应、平民逃窜改为 move_done 到达驱动。
- 文档：RA §13 #10 定案、§14 R10、阶段 3 勾选与验收记录。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] ctest：94 用例 / 548 断言全绿（阶段 2 基线 82/430 → +12/+118）
- [x] smoke_town 11 链路 ×3 PASS（含 move_done 到达驱动的巡逻/响应/逃窜）；
      其余四套冒烟回归全绿

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | GridNav/MoveDone 属 testing 测试设施；Godot 经框架公共接口消费；框架核心零改动 |
| B 线程 | 通过 | 全部【驱动线程】；异步化策略按 §3.2 契约记录（R10-3） |
| C 内存性能 | 通过 | A* 单次 <0.1ms（38×21 网格）；路径逐段行进常数级 |
| D 注释命名 | 通过 | 确定性/复杂度/线程契约注释完整；obstacle_version 语义明确 |
| E 冗余 | 通过 | 无死代码；move_target_ 清理（路径化后单点路径统一）；can_reach 同单元语义补正 |
| F 测试 | 通过 | GridNav 8 用例（含负坐标 origin）+ 阶段3 验收 4 用例 + Godot 冒烟覆盖 |

## 4. 问题与修复（实现过程中发现并闭环）

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | MockBody 瞬移使"tick 后规划"起终点同格 → 路径恒空 | 验收语义 | 宿主规划以"上一目标"为起点（测试）；Godot 侧实时位置规划天然无此问题 | 已修复 |
| 2 | can_reach 同单元返回 false | 语义 | 同单元 = 可达（已就位） | 已修复 |
| 3 | 世界原点居中导致负坐标出界 | 场景坐标 | GridNav::set_origin（负坐标可映射） | 已修复 |
| 4 | 演示 head 文件 draw_map 声明重复（编辑批次错位） | CS-§4 | 去重 | 已修复 |

## 5. 结论

☑ 通过，可 push
