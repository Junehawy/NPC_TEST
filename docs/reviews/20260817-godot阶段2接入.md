# 审查报告：Godot 演示接入阶段 2 行为系统（2026-08-17）

## 1. 变更范围

- `demo_config.{h,cpp}`：新增可选 `fsm` 段（enabled/definition/stimulus_window_seconds/
  player_near_distance，fail-fast：enabled=true 必须带 definition）；`check_known_keys`
  模板化（支持不同尺寸键表）。
- `npc_agent_demo_node.{h,cpp}`：双模式装配——`fsm.enabled` 时使用框架
  `FsmDecisionMaker` + `PerceptionModule`（决策表 #35 on_tick 钩子），否则保持旧巡逻
  决策器+玩具能力；FSM 模式跳过 greet/感知半径交叉校验（greet 参数不参与）；
  注入 `player_distance` + `player_near` 旗标；setup_agent 改返回 bool（FSM 定义校验失败
  即拒绝装配）；FSM 模式提示语。
- `player.gd` + 演示节点：钳制边界 `clamp_size` 改由配置注入——修复无头模式首帧视口
  64×64 导致玩家被错误钳到 (40,40) 的缺陷（旧冒烟靠后期传送玩家掩盖了该问题）。
- `assets/npcs/sample_guard_fsm.json`（新增）：FSM 定义（idle→alert→call_support→
  search→greet 行为链）+ 感知窗 3s + 近距 2u。
- `scripts/smoke_fsm.gd`（新增）：五链路时间线断言（待机/惊吓/呼叫支援/搜寻/问候）。
- 文档：RA §14 R7-10。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] 四套冒烟全 PASS（fsm ×3 + 默认/alt/负向各 1）
- [x] 窗口化实测：核显渲染 + `装配阶段 2 行为系统（FSM + 感知模块）`，guard_fsm 就绪

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 演示经框架公共 API（decision/capabilities）接入，无私有依赖 |
| B 线程 | 通过 | 与既有演示同一驱动线程模型 |
| C 内存性能 | 通过 | FSM 模式仅装配期解析；运行期每 tick 常数级 |
| D 注释命名 | 通过 | 双模式语义、旗标注入、配置字段均注释 |
| E 冗余 | 通过 | 旧模式保留（既有冒烟/配置不回退）；调试脚本已清理 |
| F 测试 | 通过 | smoke_fsm 五链路断言；旧模式四套冒烟回归无退化 |

## 4. 问题与修复

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | greet/感知半径交叉校验误伤 FSM 模式（greet 不参与） | CS-§9 | 交叉校验限定旧模式 | 已修复 |
| 2 | 无头模式首帧视口 64×64，玩家被钳到 (40,40) | TS-§1 测试可见性 | clamp_size 配置注入，不读瞬时视口 | 已修复 |
| 3 | check_known_keys 键表尺寸写死 8 | CS-§4 | 模板化 | 已修复 |
| 4 | setup_agent 失败路径被忽略（FSM 定义错误仍置 ready） | CS-§9 | 改返回 bool，失败拒绝装配 | 已修复 |

## 5. 结论

☑ 通过，可 push
