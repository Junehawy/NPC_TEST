# 审查报告：Godot 多 NPC 场景（2026-08-17）

## 1. 变更范围

- `demo_config.{h,cpp}`：`scene.npcs` 多 NPC 规格表（NpcSpec：name/spawn/tint/rng_seed/
  shout_when_say/独立 FSM），fail-fast（名称唯一非空、tint 三数值、多 NPC 模式 FSM 必须
  启用且带定义）；parse_fsm 前向声明。
- `npc_agent_demo_node.{h,cpp}`：多 Agent 装配——每 NPC 独立 Agent + FsmDecisionMaker +
  PerceptionModule + GodotBody/气泡/染色精灵；宿主注入 per-NPC player_distance/player_near
  旗标；`propagate_shouts()` 声学传播（台词含"呼叫支援"→ stimulus.shout，边沿触发）；
  调试面板多 NPC 逐行；单 NPC 旧模式（巡逻/FSM）保持兼容。
- `assets/npcs/sample_town.json`（新增）：三 NPC 剧本——守卫（巡逻/警戒/呼叫支援/搜寻/
  问候 + 吼叫广播）、支援兵（隐蔽/响应集合点，响应迁移先于隐蔽以规避枪声旗标阻塞）、
  平民（闲逛/逃窜/问候）。
- `scripts/smoke_town.gd`（新增）：八链路断言（巡逻/警戒/逃窜/隐蔽/呼叫/响应/搜寻/问候）。
- 文档：RA §14 R7-11。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] smoke_town 八链路 ×3 PASS；smoke_test/smoke_fsm/smoke_alt/smoke_bad 回归全绿
- [x] 窗口化实测：核显渲染 + `装配阶段 2 多 NPC 行为系统`，3 个 NPC 就绪

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 多 Agent 经框架公共 API（AgentSystem 多 Agent + 独立黑板/身体） |
| B 线程 | 通过 | 单驱动线程（Godot 主线程）驱动全部 Agent |
| C 内存性能 | 通过 | 每 NPC 常数级状态；面板字符串每帧重建（演示规模） |
| D 注释命名 | 通过 | NpcSpec 字段、声学传播、旗标注入均注释契约 |
| E 冗余 | 通过 | 单/多模式共用 WorldTransform/身体绑定/旗标注入路径 |
| F 测试 | 通过 | 八链路断言 ×3；旧模式四套冒烟无退化；fail-fast 校验路径保留 |

## 4. 问题与修复

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | parse_fsm 定义顺序晚于 parse_npc | CS-§4 | 匿名命名空间前向声明 | 已修复 |
| 2 | 守卫 idle 2s 使巡逻在枪声时刻才出现 | 演示语义 | idle → walk1 缩至 1.0s | 已修复 |
| 3 | 支援兵"隐蔽"迁移先于"响应"，枪声旗标（窗 5s）持续阻塞响应 | 演示语义 | 迁移顺序调整（响应优先） | 已修复 |
| 4 | 搜寻目标 7 单位远，相位内走不到玩家近距 | 演示语义 | 目标 (2.2,0) + 相位 3.5s | 已修复 |

## 5. 结论

☑ 通过，可 push
