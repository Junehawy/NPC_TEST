# 审查报告：环境安装脚本 setup-env.sh（2025-08-15）

## 1. 变更范围
- 新增 `scripts/setup-env.sh`：一键安装/验证开发环境（幂等）——构建工具（g++≥13 / cmake≥3.28 / ninja / git）、clang 工具链（clang-format/clang-tidy）、sanitizer 运行库（libasan/libubsan，冒烟编译验证）、可选 `--fix-ssh`（修复 /etc/ssh/ssh_config.d 属主，实测病灶为 nobody:nobody）、可选 `--bootstrap`（预热 FetchContent + 构建 + ctest）、`--dry-run` 预览。
- 修改 `README.md`（环境安装章节）、`AGENTS.md`（环境准备入口）。

## 2. 门禁结果
- [x] check-gate.sh 全绿（依赖方向 ok / 构建 -Werror ok / ctest 4/4 / 格式 skip）
- [x] `bash -n` 语法检查通过；`--dry-run` 实测：正确诊断本机三项缺口（clang 工具、libasan、SSH 目录属主）且预览动作准确
- [x] ASan/UBSan：脚本本身无 C++ 变更，不适用

## 3. 清单核对（review-process §3 A~F）
| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 纯 shell 脚本，不触碰 C++ 依赖方向 |
| B 线程 | 通过 | 不适用（无并发） |
| C 内存性能 | 通过 | 不适用（安装脚本）；ASan 冒烟测试临时文件即用即删 |
| D 注释命名 | 通过 | 头注释含用途/用法/支持发行版；函数命名清晰；`set -euo pipefail` |
| E 冗余 | 通过 | 幂等检查避免重复安装；无死分支 |
| F 测试 | 通过 | `--dry-run` 实测三缺口诊断正确；实际安装路径需 root，待用户本机执行验证 |

## 4. 问题与修复
| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | dry-run 模式下打出误导性"安装后仍不可用"警告 | 输出可靠性 | 安装后验证逻辑加 DRY_RUN 分支，预览与实测语义分离 | 已修复 |
| 2 | chmod 触发文件变更导致 edit 需重读 | 工具流程 | 重读后应用（非代码问题） | 已处理 |

## 5. 结论
☑ 通过，可 push。
注：本环境 sudo 被 no-new-privileges 封锁，`--dry-run` 之外的实装路径（dnf 安装、SSH 权限修复）需用户在真实权限环境执行：`sudo ./scripts/setup-env.sh --fix-ssh --bootstrap`。
