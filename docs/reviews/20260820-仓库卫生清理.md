# 审查报告：仓库卫生清理——.tmp 移除与插件过滤（2026-08-20）

## 1. 变更范围

- 删除仓库根 `.tmp-wall/`（外部杂项图片，非本项目产物）。
- `.gitignore` 新增：`game_adapter/godot_demo/addons/`（第三方 Godot 插件不入库，
  如 ziva_agent/CEF 约 444MB 二进制；注释说明如需入库某插件如何解锁）、
  `.tmp-*/`（临时/外来目录统一过滤）。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] 过滤规则验证：`git check-ignore` 确认 addons/ 与 .tmp-* 均被忽略；
      工作区仅 `.gitignore` 一处改动

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 仅仓库规则变更，无代码路径 |
| B~E | 通过 | 不涉及 |
| F 测试 | 通过 | 门禁回归全绿；规则经 check-ignore 验证 |

## 4. 背景

阶段3 推送时误把工作区外来文件（.tmp-wall/、addons/ziva_agent/）`git add -A` 打包，
遭远端拒绝后已撤销重推。本次为根因治理：删除临时目录 + 通用过滤规则，
防止同类误提交再次发生（后续提交应继续用显式路径暂存，避免 -A）。

## 5. 结论

☑ 通过，可 push
