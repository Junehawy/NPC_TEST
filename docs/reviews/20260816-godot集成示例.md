# 审查报告：Godot 2D 集成示例（2026-08-16）

## 1. 变更范围

- `game_adapter/godot_demo/`（新增）：Godot 4.7 GDExtension 工程——
  - `src/`：`GodotWorld`（IWorld：Node2D 实体投影/时间推进/刺激日志）、`GodotBody`
    （IAgentBody：持续移动/气泡台词与表情）、`NpcAgentDemoNode`（场景根节点：
    配置加载 → AgentSystem 装配 → 每帧驱动 → 调试面板）、注册与扩展入口；
  - `scenes/main.tscn`、`scripts/player.gd`（GDScript 输入，跨语言调用 C++ 节点）、
    `scripts/smoke_test.gd`（无头冒烟断言）、`project.godot`、`.gdextension`、精灵/配置资源。
- `CMakeLists.txt`：`NPC_AGENT_BUILD_GODOT_DEMO` 选项 + godot-cpp 4.5-stable FetchContent +
  `npc_agent_godot_demo` 共享库目标（自身 -Werror）；`CMakePresets.json` 新增 `godot` 预设。
- `scripts/check-gate.sh`：新增第 5 步 Godot 演示冒烟（可选，未装 godot 自动跳过）。
- `npc_agent/testing/intent_desc.{h,cpp}`：意图描述提取为共享工具（无头示例与
  Godot 演示共用，消除重复）；`game_adapter/sample_adapter/main.cpp` 改用它。
- 文档：RA §8.2 目录树、§14 R7 变更记录；AGENTS.md 环境节补充 Godot 构建/运行/冒烟。
- 未变更接口层（RA-§3），核心库零改动（仅 testing 模块新增工具）。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿（依赖方向 / -Werror 构建 / ctest 6/6 / 格式 / Godot 冒烟
      `[smoke] PASS startle=true greet=true`）
- [x] 窗口化运行验证：Godot 4.7.1 加载扩展、`NPC 智能体已就绪：guard_1`、场景实时运行

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | godot-cpp/Godot 类型仅存在于 game_adapter 层；门禁依赖方向 grep 覆盖 godot；演示构建开关隔离 |
| B 线程 | 通过 | 单线程（Godot 主线程=驱动线程）；全部新增接口注释带【驱动线程】标签 |
| C 内存性能 | 通过 | 无裸 new/delete（memnew/树所有权）；PIC 仅在 godot 预设开启，不影响门禁产物；线性扫描有复杂度注释 |
| D 注释命名 | 通过 | 新增公共 API 均有用途/契约注释；常量 k 前缀；无魔数 |
| E 冗余 | 通过 | 中途移除两处 YAGNI（调试节流、动作日志环）；意图描述去重；错误路径 fail-fast（配置缺失/解析失败禁用驱动并报错） |
| F 测试 | 通过 | 新增行为有冒烟断言（startle/greet 链路）并入门禁；现有 36 用例 171 断言无回归 |

## 4. 问题与修复（实现过程中发现并闭环）

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | 适配器命名空间 `adapter::godot` 遮蔽全局 `godot` 命名空间 | CS-§4 | 改名 `adapter::godot_demo` | 已修复 |
| 2 | 成员函数内 `to_string` 命中 `godot::Object::to_string()`（成员优先于 ADL） | — | 显式限定 `npc_agent::core::to_string` | 已修复 |
| 3 | GDExtension 以 `-fno-exceptions` 编译，try/catch 编译失败 | 红线 4 | nlohmann 非抛解析接口（fail-fast） | 已修复 |
| 4 | godot-cpp 默认 `-static-libstdc++` 而本机无静态库 | — | `GODOTCPP_USE_STATIC_CPP=OFF`（注释说明发布期可改静态） | 已修复 |
| 5 | 官方编辑器二进制为 debug+editor 构建，`linux.release.x86_64` 键不匹配 | — | `.gdextension` 声明 debug.editor/debug/release 三键映射同一产物 | 已修复 |
| 6 | 演示缺 alarm 机制导致决策器恒权威、能力链路不可见 | RA-§3.4 演示语义 | 枪声置黑板 alarm（pending）3 秒，展示完整仲裁 | 已修复 |

## 5. 结论

☑ 通过，可 push
