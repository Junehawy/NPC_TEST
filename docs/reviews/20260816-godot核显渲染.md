# 审查报告：Godot 演示核显渲染（2026-08-16）

## 1. 变更范围

- `scripts/run-godot-demo.sh`（新增）：演示启动入口——检测 `/dev/dri` 后显式选用
  Intel 核显（iris），无设备节点时回退软渲染并提示；扩展库缺失时给出构建指引。
- `AGENTS.md` 环境节：运行方式改为脚本入口，附核显生效判定（日志 `Using Device:` 行）。
- RA §14 新增 R7-8：硬件渲染环境说明（Optimus 双显卡 + 沙箱差异）。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] 实机验证：danger-full-access 下运行 Godot，日志
      `Using Device: Intel - Mesa Intel(R) UHD Graphics (CML GT2)`（iris 硬件渲染生效）；
      对照先前沙箱内日志为 `llvmpipe`（软件渲染）

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 仅脚本与文档，无代码路径变更 |
| B 线程 | 通过 | 不涉及 |
| C 内存性能 | 通过 | 无运行时开销；环境变量在进程启动时设置 |
| D 注释命名 | 通过 | 脚本头注释说明用途与判定方法 |
| E 冗余 | 通过 | 无死代码；错误路径（缺库/无设备）均有提示 |
| F 测试 | 通过 | 既有冒烟/门禁无回归；硬件渲染为环境层验证（无头环境不可自动复现） |

## 4. 问题与修复

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | 演示在 llvmpipe 软件渲染下运行，本机核显闲置 | — | 排查确认沙箱隐藏 /dev/dri（用户终端默认可见）；脚本显式选 iris + 回退提示 | 已修复 |

## 5. 结论

☑ 通过，可 push
