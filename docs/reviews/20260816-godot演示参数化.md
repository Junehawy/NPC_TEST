# 审查报告：Godot 演示全参数化（2026-08-16）

## 1. 变更范围

- `game_adapter/godot_demo/src/demo_config.{h,cpp}`（新增）：DemoConfig 参数集与
  fail-fast 解析器——全部演示行为参数收拢至 NPC 配置 `extra.demo` 段（框架透传 extra，
  不侵入 npc_agent）；未知键/类型错误/非法值（非正时长、空巡逻点、越界窗口）启动报错。
- 参数化能力（新增/改写，替代演示中的玩具能力）：`greet_capability.h`
  （开关/文案/语气/优先级/触发距离）、`startle_capability.h`（刺激类型/表情/优先级）、
  `alert_capability.h`（表情/优先级）。
- `guard_patrol_decision.{h,cpp}`：PatrolParams（回路/时间片/移速/让位开关）。
- `godot_body.{h,cpp}`：BodyParams（气泡时长/到达阈值）。
- `npc_agent_demo_node.{h,cpp}`：配置驱动装配（窗口尺寸/缩放/出生点/面板开关）、
  黑板注入 `player_distance`（问候距离判定）、配置选择优先级
  环境变量 `NPC_DEMO_CONFIG` → 命令行 `--config` → 默认、交叉校验（问候距离 ≤ 感知半径）。
- `player.gd`：speed/clamp_margin 脚本变量（C++ 注入）+ 视口动态钳制。
- 配置资产：`sample_guard.json`（完整参数表，值=原行为）、`sample_guard_alt.json`
  （800×450/scale 80/快巡逻 4u/s/自定义问候语/短警戒）、`sample_guard_bad.json`（负向夹具）。
- 冒烟：`smoke_alt.gd`（配置驱动坐标计算）、`smoke_bad.gd`（fail-fast 负向断言）。
- 文档：RA §14 R7-9、AGENTS.md 多配置冒烟与参数说明。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] 多轮测试 9/9：默认/alt/负向冒烟各 3 轮全 PASS
- [x] Movie Maker 量化验证：alt 布局经视口拉伸换算后与配置精确吻合
      （player spawn 680,225 → 画布 816,270 = ×1.2）；patrol.speed 实测
      alt 319px/s（预期 320）、default 198px/s（预期 200）

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 参数经框架 extra 透传，npc_agent 零改动；能力全部在 game_adapter 层 |
| B 线程 | 通过 | 装配期一次性解析；运行期仅读参数副本，无共享可变状态 |
| C 内存性能 | 通过 | 参数副本常量级开销；冒烟坐标配置驱动，无硬编码像素 |
| D 注释命名 | 通过 | 参数字段逐项注释；解析错误含字段路径；优先级链注释完整 |
| E 冗余 | 通过 | 玩具能力在演示中不再重复使用（保留供测试/无头示例）；无死代码 |
| F 测试 | 通过 | 三套冒烟 ×3 轮；负向 fail-fast 断言；参数效果像素级量化验证 |

## 4. 问题与修复（实现过程中发现并闭环）

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | 参数化重构丢失 walk 阶段初始化 → 先歇后走 | CS-§9 | 构造时 phase_left_ = walk_seconds | 已修复 |
| 2 | get_cmdline_user_args 返回 PackedStringArray | — | 按 4.5 类型修正 | 已修复 |
| 3 | Movie Maker 模式不转发用户参数 → alt 配置未生效 | CS-§9 | 增加环境变量 NPC_DEMO_CONFIG 通道（优先级最高） | 已修复 |
| 4 | 负向冒烟 get_node 在未装配场景上抛错挂起 | TS-§1 | get_node_or_null + 空面板断言 | 已修复 |
| 5 | 视口 800×450 拉伸进 960×540 画布导致像素分析误判 | — | 换算拉伸因子后重新量化验证 | 已修复 |

## 5. 结论

☑ 通过，可 push
