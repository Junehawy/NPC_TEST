# 审查报告：VS Code IDE 配置修复（2025-08-16）

## 1. 变更范围
- `.vscode/c_cpp_properties.json`：includePath 补三方依赖路径（`build/_deps/json-src/include`、`build/_deps/catch2-src/src`）+ 编译期宏 defines；compileCommands 保留为主来源。
- `.vscode/settings.json`：移除 `C_Cpp.default.configurationProvider`（cmake-tools provider 会覆盖 c_cpp_properties 导致智能感知丢路径）；`*.h` 语言关联改为 cpp。
- `.vscode/tasks.json`：默认构建任务改为 `CMake: Build`；单文件 g++ 任务降为非默认并标注"缺三方 include 不推荐"。
- `.gitignore`：解除 `.vscode/` 忽略，IDE 配置随仓分发（换机器零配置）。
- （launch.json 无需改动：F5 已正确指向 build/npc_test + preLaunchTask + 仓库根 cwd）

## 2. 门禁结果
- [x] check-gate.sh 全绿（本次为配置变更，构建/测试/格式无回归）
- [x] 三份 JSON 语法校验通过
- [x] compile_commands.json 实测包含 `-I.../build/_deps/json-src/include`（CMake 侧无问题）

## 3. 根因与说明
- 构建/测试通过的原因：编译器经由 CMake 生成的 `-I` 参数找到三方头文件（依赖在 `build/_deps/`，由 FetchContent 按锁定版本下载，**非机器全局安装**）。
- IDE 飘红/直接 run 失败的原因：cpptools 智能感知与"Run C++ File"不读 CMake 参数；原配置的 includePath 只有已废弃的 `src/**`，且 configurationProvider 覆盖了 compileCommands。

## 4. 结论
☑ 通过，可 push。用户侧需执行一次 "C/C++: Reset IntelliSense Database" 并重载窗口以清缓存。
