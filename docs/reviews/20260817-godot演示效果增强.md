# 审查报告：Godot 演示效果增强（NPC 区分/多次开枪/装饰地图）（2026-08-17）

## 1. 变更范围

- 三套独立 NPC 造型 SVG（guard 盾牌/medic 红十字/civilian 草帽）+ `NpcSpec.sprite`
  路径参数 + 头顶常驻名牌（spec 染色，font_size 14）。
- 多次开枪修复：greet/respond 状态补充 heard_gunshot 中断迁移（问候→警戒/逃窜、
  响应→隐蔽）；曾尝试"警戒/逃窜自迁移持续重触发"但发现电平旗标下自迁移会每 tick
  重置相位计时导致链条推后到感知窗之外——已回退，仅保留跨状态中断（正确修法）。
- `scene.map_enabled` 装饰地图：草地/道路中线/五栋建筑/五棵树木（纯视觉无碰撞，
  注释标明碰撞与寻路属阶段 3）；`draw_map()` 实现。
- `smoke_town.gd` 新增阶段 6：二次开枪回归（守卫重入警戒/平民再逃窜/支援兵再隐蔽）。
- godot-cpp 4.5 头文件名修正（polygon2d.hpp 无下划线）。
- 文档：RA §14 R7-12。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿
- [x] smoke_town 11 链路 ×3 PASS（含二次开枪重响应）；其余四套冒烟回归全绿
- [x] 窗口化实测：新造型/名牌/地图正常渲染（导入缓存后无加载错误），3 NPC 就绪

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 纯适配层与资源变更，框架零改动 |
| B 线程 | 通过 | 不涉及 |
| C 内存性能 | 通过 | 地图节点一次性构建；名牌/精灵为场景常量 |
| D 注释命名 | 通过 | sprite/map_enabled 字段与中断语义注释完整 |
| E 冗余 | 通过 | 自迁移方案已回退（错误路径清理）；三套 SVG 各有用途 |
| F 测试 | 通过 | 二次开枪回归断言覆盖用户报障场景 |

## 4. 问题与修复

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | NPC 仅靠调色区分，辨识度低 | 演示语义 | 独立造型 + 常驻名牌 | 已修复 |
| 2 | 多次开枪后守卫/支援兵/平民无反应（greet/respond 无枪声中断） | 演示语义 | 反应后置状态补 heard_gunshot 中断迁移 | 已修复 |
| 3 | 自迁移持续重触发方案致相位计时每 tick 重置、链条推后 | 确定性语义 | 回退自迁移，保留跨状态中断 | 已修复 |
| 4 | 地图空白 | 演示语义 | 装饰地图（无碰撞，阶段 3 接寻路） | 已修复 |

## 5. 结论

☑ 通过，可 push
