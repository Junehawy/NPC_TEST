# 审查报告：阶段 1 核心架构（2025-08-16）

## 1. 变更范围
- **接口层落码**（RA-§3 冻结契约 → 真实头文件）：`interfaces/types.h`（POD 契约 + json 别名）、`memory_event.h`、`serializable.h`、`i_world.h`（PerceptionQuery/PerceivedEntity/Stimulus）、`i_agent_body.h`（DialogueLine/GameEvent）、`intent.h`（variant 负载 + ready/async_token）、`icapability.h`（ICapability + IDecisionMaker 纯标记）、`i_llm_provider.h`（AsyncToken + cancel 默认空操作）。
- **核心运行时**：`core/event.h`（AgentEvent ADL）、`blackboard.h/.cpp`（透明比较 + ISerializable）、`agent_config.h/.cpp`（fail-fast 解析）、`capability_factory.h/.cpp`（存档恢复注册表）、`agent.h/.cpp`（仲裁管线四步 + 根 RNG 派生 + 复用缓冲）、`agent_system.h/.cpp`（感知注入 R5-3、快照组装、全局广播、restore）。
- **测试设施**：`testing/mock_world.h/.cpp`（线性扫描、刺激日志、快照环）、`mock_body.h`（动作记录）、`toy_capabilities.h`（巡逻决策器/受惊/问候三种玩具模式）。
- **测试**：新增 22 用例（仲裁四步、确定性、私有事件、感知注入、读档恢复、失败路径、配置校验、黑板序列化）。
- **示例**：`sample_adapter/main.cpp` 全链路无头演示 + `assets/npcs/sample_guard.json`。
- **文档**：RA §8.2 目录（testing/ 移位）、阶段 1 勾选 + 验收记录、§14 R6 六条实现澄清。

## 2. 门禁结果
- [x] check-gate.sh 全绿：依赖方向 / 构建 -Werror 零警告 / ctest 5/5 / **clang-format 检查启用并通过**（全部新代码已格式化）
- [x] Release 构建：36 用例 / 171 断言
- [x] Debug + ASan/UBSan（gcc，本机 libasan 已就绪）5/5 全绿
- [x] 冒烟示例：巡逻（确定性扰动）→ 枪声事件 → alarm 置位后决策器 pending → 兜底 SayIntent 全链路正确

## 3. 清单核对（review-process §3 A~F）
| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 接口层仅 POD+std+nlohmann；icapability→core 为框架内部依赖（注释声明）；MockWorld 移入 testing/ 使 npc_agent/tests 零 game_adapter 依赖（R6-1） |
| B 线程 | 通过 | 全部公开 API 标注【驱动线程】；parse 类函数标注【任意线程】；无工作线程引入 |
| C 内存性能 | 通过 | 仲裁候选/全局事件复用缓冲（CS-§7.5）；deque::swap 排空事件零分配；Intent 负载拷贝已注释说明；map 透明比较免临时 string |
| D 注释命名 | 通过 | 公开 API 五要素齐全；命名符合 CS-§4；档案字段与解析对称 |
| E 冗余 | 通过 | 无死代码；玩具能力三模式各演示一类用法，无重复 |
| F 测试 | 通过 | 仲裁四步全覆盖 + 确定性 + 读档恢复 + 失败路径定位；clang-format 门禁生效 |

## 4. 问题与修复
| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | `map::erase(string_view)` 需 C++23，C++20 编译失败 | 编译基线 CS-§2 | 改 find + 迭代器删除，注释说明 | 已修复 |
| 2 | rng_seed 校验误拒正整数（nlohmann 解析为有符号） | 测试失败 | 同时接受有符号非负与无符号表示 | 已修复 |
| 3 | 存档失败路径测试断言对象错误（首个缺失是 toy_greet 非 toy_patrol） | 测试正确性 TS-§4.2 | 断言改为实际首个缺失能力 | 已修复 |
| 4 | clang-format 门禁首跑即拦截 main.cpp 排版 | CS-§3 | 全部新代码 clang-format -i 归一 | 已修复 |
| 5 | blackboard.h 缺 `<functional>`（std::less） | IWYU CS-§6.1 | 补 include | 已修复 |

## 5. 结论
☑ 通过，可 push
