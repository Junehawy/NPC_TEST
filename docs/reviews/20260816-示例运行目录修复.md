# 审查报告：示例运行目录修复（2026-08-16）

## 1. 变更范围

- `game_adapter/sample_adapter/main.cpp`：配置路径改为三级定位（argv → 相对 CWD → 源码根兜底），移除"必须在仓库根目录运行"限制；`main` 改为 `main(argc, argv)`；错误信息给出修复建议。
- `CMakeLists.txt`：向 `npc_test` 注入 `NPC_TEST_SOURCE_ROOT` 编译宏；新增回归测试 `npc_test_anydir_smoke`（`WORKING_DIRECTORY=${CMAKE_BINARY_DIR}`，复现"从 build/ 直接运行"场景）。
- 不涉及 `npc_agent/` 接口层与 RA 文档（RA-§3 未变更）。

## 2. 门禁结果

- [x] check-gate.sh 全绿（build -Werror / ctest 6/6 / 格式）
- [x] 手动复现：`build/` 目录直接运行 exit 0；命令行参数显式指定路径运行正常

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 仅改示例适配层；核心库无感知；兜底路径通过编译宏注入，无运行时路径推断 |
| B 线程 | 通过 | 无并发改动 |
| C 内存性能 | 通过 | 候选探测为启动期一次性小开销（<10 次 fopen），非热路径 |
| D 注释命名 | 通过 | 新增 `kRelativeAssetPath`/`locate_asset_path` 命名符合 CS-§4；注释说明三级优先级 |
| E 冗余 | 通过 | 无死代码；`argc/argv` 均被使用；错误处理路径完整（三候选耗尽后给出显式路径建议） |
| F 测试 | 通过 | bug 修复带复现测试（TS-§5）：`npc_test_anydir_smoke` 在构建目录运行 |

## 4. 问题与修复

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | `npc_test` 写死相对路径，CWD 非仓库根即失败 | CS-§9（错误处理路径）/ TS-§5 | 三级候选定位 + 编译期源码根兜底 + 回归测试 | 已修复 |

## 5. 结论

☑ 通过，可 push
