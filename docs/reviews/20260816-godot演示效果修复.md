# 审查报告：Godot 演示效果修复（2026-08-16）

## 1. 变更范围

- `game_adapter/godot_demo/src/guard_patrol_decision.{h,cpp}`（新增）：waypoint 回路巡逻
  决策器（walk/rest 两阶段），警戒或玩家可见时让位仲裁（RA-§3.4）。
- `game_adapter/godot_demo/src/alert_capability.h`（新增）：警戒期兜底表情（优先级梯次
  惊吓 5 > 问候 2 > 警戒 1.5）。
- `godot_body.{h,cpp}`：bind 增加精灵参数；移动朝向翻转；惊吓展示锁（1.2s 内台词不打断）；
  say 停下（交谈语义）。
- `npc_agent_demo_node.cpp`：NPC 初始站位=世界原点（修复渲染在角落的 bug）；新决策器/
  警戒能力装配；窗口 960×540；注册 group `npc_demo`（GDScript 定位）。
- `player.gd`：group 定位演示节点（autopilot 嵌套场景兼容）+ 窗口边界钳制。
- `smoke_test.gd`：四链路断言（巡逻/问候/惊吓/警戒）。
- `autopilot.gd` + `scenes/main_autopilot.tscn`（新增）：固定时间线自动演示（追 NPC → 问候 → 开枪）。
- 配置：感知半径 10 → 2 单位（否则全屏可见、NPC 永不巡逻）。
- 文档：RA §14 R7-4/R7-5/R7-7 更新。

## 2. 门禁结果

- [x] check-gate.sh 5/5 全绿（含 Godot 冒烟 `PASS patrol=true greet=true startle=true alert=true`）
- [x] Movie Maker 录帧像素级验证：帧 90 巡逻（NPC 位于 waypoint，玩家于出生点）；
      帧 330 问候（气泡白像素 229 > 基线 91）；帧 384 惊吓（气泡白像素 268）

## 3. 清单核对（§3 A~F）

| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | 全部改动在 game_adapter 层；框架零改动；依赖方向门禁绿 |
| B 线程 | 通过 | 单线程（Godot 主线程=驱动线程）；注释契约完整 |
| C 内存性能 | 通过 | 无新增热路径分配；气泡/移动为常数操作 |
| D 注释命名 | 通过 | 新增类/常量带用途注释；k 前缀常量 |
| E 冗余 | 通过 | 调试脚本/调试打印已清理（git diff 无残留）；autopilot 复用于视觉验收 |
| F 测试 | 通过 | 冒烟四断言覆盖新行为；既有 36 用例 171 断言无回归 |

## 4. 问题与修复（用户反馈驱动）

| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | NPC 节点未设初始位置，渲染于屏幕左上角（用户"效果不理想"主因） | CS-§9 | 初始站位=世界原点（transform.origin） | 已修复 |
| 2 | 玩具抖动巡逻（每 tick 随机目标）视觉像抽搐 | 演示语义 | GuardPatrolDecision waypoint 回路 + walk/rest | 已修复 |
| 3 | 问候仅警戒期触发，提示语误导 | 演示语义 | 巡逻在玩家可见时让位仲裁，靠近即问候 | 已修复 |
| 4 | 感知半径 10 单位覆盖全屏，NPC 永不巡逻 | 演示语义 | 感知半径调至 2 单位 | 已修复 |
| 5 | player.gd 绝对路径 `/root/Main` 在嵌套场景失效 | CS-§9 | group `npc_demo` 定位 | 已修复 |
| 6 | 惊吓表情 1 tick 即被台词覆盖，肉眼不可见 | 演示语义 | 惊吓展示锁 1.2s | 已修复 |

## 5. 结论

☑ 通过，可 push
