# 审查报告：阶段 0 工程底座（2025-08-15）

## 1. 变更范围
- `CMakeLists.txt`：FetchContent 集成 Catch2 v3.8.1 + nlohmann/json v3.11.3（版本锁定）；编译期开关选项（`NPC_AGENT_ENABLE_LLM` / `NPC_AGENT_ENABLE_MEMORY_VECTOR` / `NPC_AGENT_SANITIZE`）；`npc_agent` 库 + 冒烟程序 + 四个测试二进制（含开关矩阵三行独立重编）。
- `npc_agent/config/feature_flags`（h/cpp）：三级开关校验——编译期×运行期超集约束，fail-fast 定位错误（RA-§5.2）。
- `npc_agent/tests/`：主测试 14 例 + 矩阵 {0,1}/{1,0}/{0,0} 三二进制 11 例；`static_assert` 守卫每个二进制对应的矩阵行。
- `game_adapter/sample_adapter/main.cpp`：冒烟程序（对齐 RA-§8.2 目录）。
- 文档同步：RA 阶段 0 勾选 + 验收记录；CS-§4/§5 两处条款细化（公开 POD 字段例外、线程标签补【任意线程】档）；`.gitignore` 补 `build-*/`；门禁脚本格式检查改为"暂存+未暂存并集"。

## 2. 门禁结果
- [x] check-gate.sh 全绿（依赖方向 ok / 构建 -Werror 零警告 / ctest 4/4 / 格式 skip：clang-format 未安装）
- [x] Release 构建 ctest 4/4：25 用例、69 断言
- [x] Debug + ASan/UBSan（clang-22）ctest 4/4 全绿（环境无 gcc libasan 运行库，见 §4-#2）

## 3. 清单核对（review-process §3 A~F）
| 组 | 结果 | 备注 |
|---|---|---|
| A 耦合 | 通过 | npc_agent 仅依赖 std + nlohmann（核心合法依赖）；无 game_adapter 反向 include；门禁自动检查 |
| B 线程 | 通过 | parse_and_validate/to_string 为纯函数，标注【任意线程】；未引入任何线程 |
| C 内存性能 | 通过 | 解析 O(n) 单次；循环内零分配；错误路径拼接仅冷路径；复杂度已注释（CS-§7.6） |
| D 注释命名 | 通过 | 公开 API 五要素齐全（含线程契约标签）；命名符合 CS-§4 |
| E 冗余 | 通过 | 无死代码/注释掉代码；冒烟程序最小化；YAGNI 遵守 |
| F 测试 | 通过 | 公开 API 全覆盖 + 矩阵 2×2 四行 + 错误路径全类型；确定性（纯函数）；用例互不依赖（TS-§4.4） |

## 4. 问题与修复
| # | 问题 | 违反条款 | 修复方式 | 状态 |
|---|---|---|---|---|
| 1 | CMakeLists 源文件路径写根相对（实际在 npc_agent/ 下） | 构建配置 | 全部改为 `npc_agent/...` 前缀 | 已修复 |
| 2 | 环境缺 gcc libasan 运行库，ASan 链接失败；sudo 被 no-new-privileges 封锁，clang-format/clang-tidy 无法安装 | 环境（非代码） | sanitize 构建改用 clang-22（运行时完整）；clang 工具待用户本机安装 | 已处理/待办 |
| 3 | 纯函数误标【驱动线程】 | CS-§5.1 | 标签体系补【任意线程】档，头文件同步 | 已修复 |
| 4 | CS-§4 成员变量尾缀未区分公开 POD 字段 | CS-§4 | 补例外条款（公开 POD 字段无尾缀） | 已修复 |
| 5 | 门禁格式检查在 git add 后漏检（git diff 默认只看未暂存） | 门禁可靠性 | 改为暂存+未暂存并集 | 已修复 |
| 6 | 冒烟程序位置与 RA-§8.2 不符；src/ 遗留编译产物二进制 | RA-§8.2 / 红线精神 | 移入 game_adapter/sample_adapter/；删除二进制 | 已修复 |

## 5. 结论
☑ 通过，可 push
